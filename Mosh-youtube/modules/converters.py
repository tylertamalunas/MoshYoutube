# this is a file that creates and stores functions that can be called on in other code files.
def lbs_to_kg(weight):
    return weight * 0.45


def kg_to_lbs(weight):
    return weight / 0.45


# this makes it so that you dont have to write the code in every file. It makes it centralized, and so that you dont
# have to write it in every code file.
# modules should contain all the related classes and functions

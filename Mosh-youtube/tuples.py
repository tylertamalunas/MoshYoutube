# tuples are like lists, but you cannot change them at all
numbers = (1, 2, 3)
print(numbers[0])

# below gives error
numbers[0] = 10

# tuples are better if you do not want to accidentally change a list
